"""Reference configurations for vehicle models, mirroring equipment/vehicle/.

One module per model, named for the model it configures. A configuration is
only meaningful against a specific set of dynamics -- the same numbers do not
describe a point mass and a two-mode engine -- so the trees are kept parallel
rather than pooling every vehicle configuration in one file.
"""
