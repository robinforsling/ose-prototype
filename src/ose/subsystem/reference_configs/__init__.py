"""Reference configurations for subsystem-layer components.

Fixtures for tests and demonstrations -- fictional and plausible, per
CLAUDE.md, not evidence that a real configuration belongs in source. One
module per subsystem component (reference_vehicle_guidance.py, ...), each
holding one or several named configurations for that component. Mirrors
equipment/reference_configs/; see its package docstring for the full
rationale (shape vs data, why Python rather than YAML today).
"""
